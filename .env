# AI Companion API Keys & Configuration
# Never commit this file. Template only — real keys stay on device Keystore.
# Round-robin: add multiple keys comma-separated per provider. Registry tries keys in order, rotates on 429/failure.

GEMINI_API_KEYS=[REDACTED]
OPENAI_API_KEYS=[REDACTED]
OPENROUTER_API_KEYS=[REDACTED]
GROQ_API_KEYS=[REDACTED]

AI_PROVIDER_ORDER=gemini,openai,openrouter,groq
AI_MODEL_DEFAULT=gpt-4o-mini
OBSERVATION_INTERVAL_MINUTES=30
